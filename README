Hey Listen!

This game was made for LD#22 in PixieEngine

Take a look in src for the game classes.

Tilemaps has all the level data.

Pixel Editor helpers
--------------------

# Fill Pure White With Static
self.eachPixel (pixel, x, y) ->
  if pixel.color().equal(Color("#FFFFFF"))
    amount = rand() * 255

    pixel.color(Color(amount, amount, amount))

